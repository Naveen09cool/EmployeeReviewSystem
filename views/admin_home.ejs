<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div id="top-div">
        <h1>Admin</h1>
        <h1><%= user.name %><%= user._id %></h1>
        <a href="/admins/view-emp">View Employee</a>
        <a href="/admins/remove-emp/id">remove Employee</a>
    </div>
    

    <div id="list-users">
        <ul>
            <%= console.log("sadasdasdwqaefds"+users.length) %>
            <% for(let i = 0; i<users.length; i++){ %>
                <li>
                    <%= users[i].name %>
                    <%if (users[i].isAdmin){ %>
                        <small>(admin)</small>
                    <%}else{%>
                        <form action="/admins/update-emp/<%= users[i].id %>" method="post">
                            <input type="submit" value="MakeAdmin">
                        </form>
                        <a href="/admins/remove-emp/<%= users[i].id %>">Delete Employee</a>
                    <%}%>
                </li>
            <%}%>
        </ul>
    </div>
</body>
</html>